{
  "routes": [
    {
      "method": "GET",
      "path": "/company-directories",
      "handler": "company-directory.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/company-directories/count",
      "handler": "company-directory.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/company-directories/:id",
      "handler": "company-directory.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/company-directories",
      "handler": "company-directory.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/company-directories/:id",
      "handler": "company-directory.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/company-directories/:id",
      "handler": "company-directory.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
